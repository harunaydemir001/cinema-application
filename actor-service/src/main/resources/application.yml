server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: actor
      authentication-database: admin
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


#  query={"status":"active"}query={"status":"passive"}


